[build-system]
requires = ["setuptools>=40.8.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = [
  "src"
]

[tool.ruff]
src = ["src"]

[tool.ruff.per-file-ignores]
"src/takenote/__init__.py" = ["F401"]
